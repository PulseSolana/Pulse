{
  "name": "pulse",
  "version": "0.1.1",
  "description": "Short-horizon order-flow pulse engine for Robinhood crypto that scores aggressive flow, buyer breadth, and liquidity migration",
  "homepage": "https://pulsesolana-launch.vercel.app/",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "zod": "^3.23.0"
  },
  "keywords": [
    "robinhood",
    "order-flow",
    "pulse",
    "momentum"
  ],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.4",
    "@vitest/coverage-v8": "^2.0.0"
  }
}
